define(function(require) {

	// var EnemyRef = require('Enemy');
	//var Enemy = new Enemy();

	// var EnemyVolRef = _.extend({
	// 	vol:function(){

	// 	}
	// }, EnemyRef);

	// return new EnemyVolRef();

	var Game = {
		// Enemy: require('Enemy'),
		init: function(){
			console.log(this);
		}
	};

	return Game;
});